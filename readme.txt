This program works with directed edge files which use "from" edges.

To compile run the following command:
$ make

To execute the subdivision program:
$ ./subdivision ../path_to/tetrahedron.diredge

After running the programs:
subdivided files are saved to the same folder as the subdivded.cpp file.

